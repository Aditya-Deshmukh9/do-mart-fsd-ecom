export default function AuthLoyout({ children }) {
  return (
    <section className="relative container min-h-screen mx-auto flex items-center justify-center p-4">
      {children}
    </section>
  );
}
